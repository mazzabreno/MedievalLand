import Phaser from "phaser";

/**
 * Stub — superseded by the React HUD layer.
 * Kept to satisfy old imports without causing compile errors.
 */
export class UIScene extends Phaser.Scene {
  constructor() {
    super({ key: "UIScene" });
  }
}
